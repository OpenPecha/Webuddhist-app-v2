/** Matches Flutter ReaderConstants.swipeVelocityThreshold */
export const SWIPE_VELOCITY_THRESHOLD = 300;

/** Matches Flutter PlanTextScreen drag threshold (20% of screen width). */
export const SWIPE_DISTANCE_RATIO = 0.2;

export type PlanReadingNavigateDirection = 'next' | 'prev' | 'finish';
